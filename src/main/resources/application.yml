spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tobi-spring?serverTimezone=Asia/Seoul
    username: root
    password:
  main:
    allow-bean-definition-overriding: true
  mail:
    host: smtp.gmail.com
    port: 587
    protocol: smtp
    default-encoding: UTF-8
    username: username
    password: password
    smtp:
      start-tls-enable: true
      auth: true
